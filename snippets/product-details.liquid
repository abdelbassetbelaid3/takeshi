<div class="product-details">

  <h1>{{ product.title }}</h1>

  {% render 'price', product: product %}

  {% form 'product', product %}

    <!-- variants -->
    <!-- quantity -->
    <!-- ATC -->

  {% endform %}

</div>